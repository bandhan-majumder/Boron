{
  "name": "boron",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "pnpm client && turbo run dev",
    "lint": "turbo run lint",
    "client": "cd packages/db && pnpm dlx prisma generate && cd ../..",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "test": "vitest"
  },
  "devDependencies": {
    "jsdom": "^27.1.0",
    "prettier": "^3.5.3",
    "turbo": "^2.5.3",
    "typescript": "5.8.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "vitest": "^4.0.6"
  }
}
